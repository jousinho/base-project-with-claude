framework:
    messenger:
        default_bus: event.bus
        buses:
            event.bus:
                default_middleware:
                    allow_no_handlers: true
        transports:
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
        routing: {}
